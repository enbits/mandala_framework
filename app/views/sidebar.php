<h3>Default Sidebar View</h3>
<p>This is the default sidebar view, loaded by all the controllers that depends on base_controller which is located in vendors/base_controller. That controller also loads the menu view and the footer view.</p>
